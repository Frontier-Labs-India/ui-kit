/* GENERATED by scripts/vendor-core.js from src/core/forms/form-types.ts — do not edit.
 * Edit the source and run `npm run vendor:core`. */

/**
 * Public form-state types, React-free so the Svelte package can vendor them
 * (scripts/vendor-core.js) and its components read the same shape a React
 * `useForm` provides. Re-exported from use-form.ts, which is where the public
 * API has always exported them.
 */

import type { FieldConfig } from './create-form.js'

export interface FieldProps {
  value: unknown
  onChange: (value: unknown) => void
  onBlur: () => void
  error: string | undefined
  touched: boolean
}

export interface FormState<T extends Record<string, FieldConfig>> {
  values: { [K in keyof T]: T[K]['initial'] }
  errors: Partial<Record<keyof T, string>>
  touched: Partial<Record<keyof T, boolean>>
  dirty: boolean
  valid: boolean
  submitting: boolean
  submitCount: number

  setValue: (name: keyof T, value: unknown) => void
  setError: (name: keyof T, message: string | undefined) => void
  setTouched: (name: keyof T, touched?: boolean) => void
  validateField: (name: keyof T) => string | undefined
  validateAll: () => boolean
  handleSubmit: () => Promise<void>
  reset: (name?: keyof T) => void
  getFieldProps: (name: keyof T) => FieldProps
}
